import { getErrorKey, getLastEvent, getSelector, getPosition } from './getters'
import {
  createJsError,
  createResourceError,
  createCorsError,
  createPromiseError
} from './factory'
import { nowTime, calcDuration } from './common'

export {
  getErrorKey,
  getLastEvent,
  getSelector,
  getPosition,
  createJsError,
  createResourceError,
  createCorsError,
  createPromiseError,
  nowTime,
  calcDuration
}
